# Basic Inventory sample

`BasicInventoryExample.cs` constructs a two-slot bag, adds stackable items, and
moves part of a stack. The sample uses only the domain package and does not require
a scene or GameObject.
