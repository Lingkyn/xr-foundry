# Inventory UI state gallery

Use `InventoryStateGallery` and the shipped nested prefabs to replay: empty,
partial, full, rejected, selected, disabled, loading, and error. Replace the nested
`InventorySlot` or `ItemView` instance with a project style/variant while retaining
the parent prefab links. This sample intentionally makes no XR or product-art claim.
