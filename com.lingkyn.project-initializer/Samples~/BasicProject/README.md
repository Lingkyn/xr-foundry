# Basic Project Scaffold

Install the package and run `Tools > Lingkyn > Project Initializer > Initialize`.
The generated `Assets/_Project` tree is intentionally empty of gameplay. Add
consumer-owned assemblies, configs, content, and adapters after validation.
