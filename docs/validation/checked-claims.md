# Checked claims register

This register lists every kind of claim the repository makes about itself and
states, for each, whether a machine checks it and which function, test, or CI job
does so. The repository is meant to run as a process that decides what merges
rather than a person who is trusted; that only works if no claim by any Agent or
person is assumed correct. A reader, a validator, or a merge verdict must be able
to tell a fact the process re-derives from a fact someone wrote down. Names below
are the real ones: `validate_*` and `check_*` functions live in
[`scripts/validate_repository.py`](../../scripts/validate_repository.py) and
[`scripts/merge_readiness.py`](../../scripts/merge_readiness.py), `test_*`
names live under [`tests/`](../../tests/), and job names come from
[`.github/workflows/`](../../.github/workflows/).

**Self-declared** means exactly this: the repository contains the claim, nothing in
the validator, the tests, or CI re-derives it, and a reader who relies on it must
verify it independently. A passing repository contract and a `ready` merge verdict
do not vouch for a self-declared claim; they prove only the machine-checked rows
on the merged tree. Every machine-checked row also names what its check does not
prove, because most checks bind structure and cross-references, not truth: a
receipt that is internally consistent and hash-bound to a public commit is still a
file someone wrote, and no check here proves that a Unity Editor produced it.

## Package identity and versions

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Package id, path, and catalog membership | `package-catalog.json`, `packages/unity/**/package.json` | Package author | `validate_repository` main body, `validate_repository_layout`, `validate_fast_structure`, `discover_lingkyn_package_paths` | Id uses the package prefix, path leaf equals id, path stays under `packages/unity`, every live `package.json` is catalogued and every catalogue entry exists, `package.json` name equals id | Nothing further; this row is fully derived |
| Package version | `package.json`, `package-catalog.json`, `foundry.component.json`, batch file, current compatibility profile | Package author | `validate_repository` main body (`catalog/package version mismatch`), `validate_component_model`, `validate_foundry_contract`, `validate_compatibility_profile_payload` (current profile equals catalog and manifest), `check_version_bump_evidence` (LESSON-008), `test_version_bump_without_catalog_and_profile_evidence_fails` | All five surfaces carry the same string; a bump in a pull request is refused unless the catalog moved and a `verified` profile records the new version in the same change | That the number is the right SemVer step for the change |
| Concrete manifest fields | `package.json` (`version`, `unity`, `unityRelease`, `dependencies`) | Package author | `validate_concrete_package_manifests`, `test_installable_manifest_rejects_dependency_range` | Exact SemVer, `major.minor` Unity floor, exact dependency versions; no ranges | That the floor is true for any editor other than the one tuple a verified profile names |
| Required package entries | `README.md`, `CHANGELOG.md`, `LICENSE.md`, `Documentation~`, `Tests`, `Samples~`, `samples` in `package.json` | Package author | `validate_repository` main body (`REQUIRED_PACKAGE_ENTRIES`) | The entries exist | Everything the documentation and samples say |
| Source hygiene | `*.cs`, `*.asmdef`, `.meta` under each package | Package author | `source_has_valid_namespace_contract`, `validate_asmdef_identity`, `validate_internal_namespace_links`, `validate_unity_asset_path_literals`, `validate_foundation_assembly_references`, `validate_inventory_isolation_rules` | Namespaces and asmdef names use the prefix, every source has a `.meta`, internal `using` targets are declared somewhere, asset paths mount by package id, foundation and Presentation assemblies reference only allowed assemblies, the Inventory authoring runtime performs no scene or `Resources` lookup | That the code compiles; only a profile receipt speaks to that |
| Changelog discipline | Root `CHANGELOG.md`, each package `CHANGELOG.md` | Change author | `check_changelogs`, `test_package_source_change_needs_package_changelog_and_reports_pending_editor_evidence`, `test_repository_level_change_needs_root_changelog` | The affected changelog file changed whenever a substantive file under that package (or a repository-level file) changed in the candidate | That the entry describes the change, sits under `Unreleased`, or matches the version it names; content is never read |
| Maturity | `package-catalog.json`, `foundry.component.json`, `reference-catalog.json` | Package author, maintainer | `validate_repository` main body (state in `maturity_states`), `validate_component_model` (component equals catalog), `validate_inventory_projection_coherence` (reference equals catalog, Inventory family only), `check_maturity_unchanged` (a change is `non_routine`) | The state is a known one and agrees between package and component records; a maturity change cannot ride a routine merge | That the maturity is earned. Nothing compares maturity with evidence. Reference-catalog maturity for non-Inventory packages is not cross-checked |
| Promotion state | `promotion` block in `package-catalog.json` | Maintainer | `validate_package_promotion`, `test_passed_candidate_rejects_pending_gate` | Internal consistency: `passed` has no pending gate, `blocked` names a failed gate and pending gates | That any gate listed under `satisfied` was in fact satisfied |
| Catalog evidence flags | `independent_compile`, `git_url_install`, `device_evidence` in `package-catalog.json` | Package author | None (only `validate_foundry_contract` requires the batch file's `device_evidence` to equal the catalog's) | The batch copy matches the catalog copy | The value itself; nothing derives these flags from profiles or receipts |
| Inventory Core public API baseline | `docs/standards/inventory/core-api-baseline.json` | Family author | `validate_inventory_api_baseline` | The listed public types equal the `public class/struct/enum/interface` declarations found by regex in `Runtime/*.cs`; package id and version match | Member-level surface, and every other family (see the API surface row under Standards) |

## Catalogs and compatibility

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Reference catalog entries | `reference-catalog.json` | Maintainer | `validate_repository` main body (package paths equal package catalog), `validate_reference_package_use_modes`, `validate_reference_evidence_paths`, `validate_component_model` (catalog pointers), `test_reference_catalog_rejects_nonexistent_evidence_path` | Every live package has exactly one reference artifact at the same path, installable artifacts list `install` and `raw_material`, every `evidence` path exists | `purpose`, `non_claims`, `kind`, and `maturity` of the non-package artifacts; that an evidence path contains evidence |
| Component catalog and colocated component manifests | `component-catalog.json`, `packages/unity/**/foundry.component.json` | Package author | `validate_component_model`, `test_component_model_rejects_package_dependency_and_lock_drift`, `test_component_model_rejects_internal_dependency_version_drift` | Schema-valid; every live package appears exactly once with a colocated manifest; id, version, and maturity agree with the package catalog and `package.json`; `contract_refs` exist; `requires` equals the internal `package.json` dependencies at the provider's exact version; no dependency cycle; slot candidates equal providers | Descriptions; that a provider honours the contract its `contract_refs` point to |
| Capability registry | `capability-registry.json` | Package author | `validate_component_model`, `test_composition_rejects_missing_and_incompatible_capability` | Schema-valid, no duplicate capability, declared providers equal the manifests' `provides`, every provided capability is registered, every required capability has exactly one provider, `contract_refs` exist | Descriptions and multiplicity semantics at runtime |
| Reference composition and lock | `compositions/unity/reference-system/` | Maintainer | `validate_component_model`, `scripts/compose_system.py --check`, `test_component_model_resolves_deterministically_and_lock_is_current`, `test_v2_composition_lock_tracks_adapter_source_bytes` | The committed lock equals a fresh resolution byte for byte, adapter sources hash-match, schema dispatch is correct | That the composition starts in a Unity player; Editor evidence for it is a profile claim (next rows) |
| Compatibility profile tuple and policy | `compatibility-profiles.json` | Evidence author | `validate_compatibility_profiles`, `validate_compatibility_profile_payload`, `test_compatibility_profiles_reject_vague_target_and_unsupported_match`, `test_current_compatibility_profiles_are_adaptive_and_exact_verified` | Schema-valid; policy constants unchanged; every axis is a concrete literal; the fifteen current profiles cover every catalogue package exactly once; `package_versions` equals the install artifact's dependency closure; requested and resolved dependencies are consistent; renderer and XR provider match the resolved graph; non-XR profiles resolve no XR package | Nothing about the tuple is proven by this row alone; the receipt binding below carries the claim |
| Verified profile claims (`editor_compile`, `editmode_tests`, `playmode_tests`, `android_build`, `device_runtime`) | `verified_claims` and `evidence` per profile, `docs/validation/evidence/<profile>/` | Evidence author | Same functions plus `validate_unity_consumer_binding`, `validate_unity_compile_result`, `validate_nunit_result`, `commit_is_public_origin_reachable`, `test_verified_compatibility_profile_accepts_only_exact_bound_evidence`, `test_compatibility_receipt_rejects_local_only_commit_with_public_tree`, `test_nunit_result_rejects_wrong_mode_missing_and_unrelated_assemblies`, `test_structured_compile_result_must_cross_bind_and_pass` | Commit is a full SHA reachable from a fetched public origin ref; receipt is schema-valid and equals the profile field for field; manifest and lock SHA-256 match the committed files; the consumer manifest pins every custom package to its canonical path at that commit; the lock's hashes, edges, depths, and external versions equal `package.json` at that commit and the profile; each verified claim maps to a passed receipt check whose evidence file exists, hashes, and (for NUnit XML and compile results) names the right mode, assemblies, and all-pass counts | That a Unity Editor produced the XML and compile result. The check proves consistency and public-commit binding, not provenance |
| Root README install matrix and every UPM selector | `README.md`, any file containing `?path=` | Documentation author | `validate_readme_git_install_matrix`, `validate_active_git_upm_selectors`, `validate_active_repository_path_references`, `test_readme_git_install_matrix_rejects_sibling_sha_drift` | Matrix covers every catalogue package and is dependency-closed, paths equal the catalog, siblings share one full SHA or the documented placeholder, no legacy root paths remain | That the SHA shown is one any profile validated |
| Repository layout contract | `docs/architecture/repository-layout.v1.json` | Maintainer | `validate_repository_layout` | Collections exist, package set equals the catalog, only the Unity engine root exists, invariants unchanged | Nothing further |
| Bug template package options | `.github/ISSUE_TEMPLATE/bug.yml` | Maintainer | `validate_bug_template_package_options` | Options equal the catalogue | Nothing further |

## Standards (source manifests, contracts, coverage maps, API surfaces)

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Inventory source manifest | `docs/standards/inventory/source-manifest.json` | Family author | `validate_inventory_source_manifest`, `test_inventory_manifest_rejects_non_positive_source`, `test_inventory_manifest_rejects_consumer_material` | Schema id, derivation and isolation policy flags, every source is `admitted_positive`, `external_public`, HTTPS, not a code seed, with an admitted authority class and non-empty publisher, title, role, evidence, limits, and license boundary | That the URL says what `positive_evidence` and `admitted_claims` attribute to it; that the license boundary is right |
| Source manifests of every other family | `docs/standards/{interaction,persistence,settings,localization,audio,locomotion,scene-flow,xr-ui-shell,design-language}/source-manifest.json` | Family author | `validate_family_source_manifests` (registered after `validate_consumer_lessons_register` in `validate_repository`), `tests/test_source_manifests.py::FamilySourceManifestTests` (for example `test_wrong_schema_is_reported`, `test_family_mismatch_is_reported`, `test_duplicate_source_id_is_reported`, `test_non_https_url_is_reported`, `test_non_public_marker_is_reported`, `test_missing_review_note_is_reported`, `test_contract_without_manifest_is_reported`, `test_manifest_without_contract_is_reported`, `test_real_repository_returns_no_errors_or_exactly_the_allowlisted_ones`); Inventory keeps its own stricter, unchanged `validate_inventory_source_manifest`. `validate_foundry_contract` and `scripts/scaffold_unity_package.py::validate_blueprint` still separately bind an admitted blueprint's `positive_sources` to this file's path | Every non-Inventory family's `schema` string matches `xr-foundry.<family>_source_manifest.v<N>` with the family segment agreeing with the directory name (hyphens as underscores); every source has a unique non-empty `id`, an `https://` `url`, and non-empty `authority`, `admitted_role`, `admitted_claims`, `excluded_uses`, `license_or_terms`, and `maintenance_evidence`; no source field names a private or consumer marker (reuses `forbidden_public_markers`); a family's `source-manifest.json` and `verification-contract.md` exist together or neither does; and a top-level or per-source `review_note` records that source URLs were not fetched, so an unfetched URL is never presented as confirmed. Any exception is named in `FAMILY_SOURCE_MANIFEST_UNVERIFIED_CLAIMS` and a stale allowlist entry is itself an error | Whether an admitted claim is actually true of the cited source, or whether a source is actually maintained, licensed, or authoritative as stated (URLs are not fetched by the check); `FAMILY_SOURCE_MANIFEST_UNVERIFIED_CLAIMS` still allowlists Design Language's missing `verification-contract.md`, Foundations' missing `source-manifest.json`, and the missing `review_note` in Interaction, Persistence, Settings, and Design Language, until those files are fixed |
| Inventory standard | `docs/standards/inventory/inventory-standard.json` | Family author | `validate_inventory_standard` | Schema id, isolation flags, the seven capabilities each cite at least two known source ids, the package family set is complete | Capability text and what the sources are said to converge on |
| Inventory projections | `inventory-standard.json`, `docs/standards/inventory/README.md`, `ROADMAP.md`, `reference-catalog.json` | Family author | `validate_inventory_projection_coherence`, `test_inventory_projection_rejects_stale_unadmitted_claim`, `test_inventory_projection_rejects_stale_xr_not_implemented_claim` | Maturity, `earliest_failed_gate`, implementation status, and dependency sets agree across the four surfaces; the exact roadmap row exists; listed stale phrases are absent | Any other prose on those pages |
| Verification contracts | `docs/standards/*/verification-contract.md` | Family author | None (the Inventory file must exist: `REQUIRED_INVENTORY_STANDARD_FILES`) | Existence for Inventory | Every clause and the claim ceiling |
| Coverage maps | `docs/standards/*/coverage-map*.json` | Family author | `validate_coverage_map_claims` (in `validate_repository`), `tests/test_coverage_claims.py::CoverageMapClaimsTests` (for example `test_unknown_test_name_is_reported`, `test_package_id_must_own_the_assembly`, `test_summary_mismatch_is_reported`, `test_unlisted_test_method_is_reported`, `test_real_repository_maps_pass_or_are_exactly_allowlisted`); `test_live_package_test_assemblies_match_recorded_evidence_counts` separately pins per-assembly case counts derived from source | Schema id; `contract` and `test_sources` paths exist; each gate's `test_assembly` names exactly one asmdef under `packages/` or `staging/` owned by the declared `package_id`; every listed test (C# `[Test]`/`[TestCase]`/`[UnityTest]` method, `validator:<function>`, or `tests/<file>.py::<Class>::<test>`) exists; `covered`/`partial`/`unmapped` agree with the `tests` and `missing` lists; clause ids unique; every attributed test method in the assembly folder is listed somewhere; `summary` counts equal a recount; any suppressed message must be named in `COVERAGE_MAP_UNVERIFIED_CLAIMS` and a stale allowlist entry is an error | That a listed test actually exercises its clause (name resolution is not semantics); `audited_at_commit` is not bound to a public commit (the localization map records `staging`); every message named in `COVERAGE_MAP_UNVERIFIED_CLAIMS`, which today allowlists entries of `docs/standards/inventory/coverage-map.json`, so those Inventory rows remain self-declared until the allowlist empties |
| API surface inventories | `docs/standards/{interaction,persistence,settings}/api-surface.md` | Family author | None (the Inventory family's JSON baseline is checked; see Package identity) | Nothing | Type counts, member lists, consumer-facing labels, binary-compatibility recommendations |
| Architecture contracts, coverage matrices, README status lines, agent review records | `architecture-contract.md`, `coverage-matrix.md`, `README.md`, `*-review.json` under `docs/standards/<family>/` | Family author, reviewing Agent | None (Inventory files must exist) | Existence for Inventory | Content, review verdicts, and status lines |
| Design language standard | `docs/standards/design-language/*.json` | Standard author | None | JSON parses | Token values, reference hierarchy, and its `proposed_pending_independent_review` status |

## Lessons register

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Register structure and family coverage | `docs/standards/lessons/lessons-register.json` | Lesson author | `validate_consumer_lessons_register`, `test_consumer_lessons_register_rejects_a_family_that_did_not_respond`, `test_consumer_lessons_register_rejects_gap_without_follow_up`, `test_consumer_lessons_register_rejects_unknown_family_and_missing_evidence`, `test_consumer_lessons_register_rejects_schema_and_policy_drift` | Schema-valid, policy flags fixed, unique ids, `origin_family` is a live family, repository evidence paths exist, exactly one disposition per live family per lesson, every `gap` or `deferred` names a `follow_up` | HTTPS evidence links are not fetched |
| Live family set | `family` field in each `foundry.component.json` | Package author | `live_package_families` (used by the function above) | The families that must respond are derived from live manifests, not typed by hand | Nothing further |
| Disposition status and rationale | Each `dispositions[]` entry | Family author | None beyond the structure above | The status is one of the schema's four | That `adopted` is true in code, that `not_applicable` is justified, that a follow-up happens; nothing compares a disposition with sources |
| Lesson text (`observed`, `rule`) | Each lesson | Lesson author | None | Non-empty per schema | The observation and the rule |

## Unity and device evidence

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Committed Editor evidence | `docs/validation/evidence/<profile>/` (receipt, compile result, consumer manifest and lock, NUnit XML) | Evidence author | Through the profile that references it (see Verified profile claims); `test_live_package_test_assemblies_match_recorded_evidence_counts` | Files referenced by a profile hash-match, cross-bind, and report all-pass for the expected assemblies; source-derived case counts match the recorded numbers | Provenance of every file; any evidence file no profile references is never opened |
| Unity consumer tests in CI | `unity-consumer-tests.yml` jobs `test-inventory`, `editor-tests`, `unity-consumer-tests`; `scripts/audit_unity_test_inventory.py`, `scripts/verify_unity_test_results.py`; `tests/test_unity_test_result_verifier.py`, `tests/test_audit_unity_test_inventory.py` | CI | The jobs themselves | For one run: the exact source-derived case count executed in one assembly per Unity process, every case passed, result newer than the run boundary | Whether the job has ever run green: nothing in the repository records a run, the job skips on forks and fails without a license secret, and a green run never updates a profile |
| Local and self-hosted gate receipts | `scripts/run_unity_gates.py` `receipt.json` (ignored by Git), `unity-self-hosted-gates.yml` job `run-gates`; `tests/test_run_unity_gates.py` | Person or Agent with an Editor | The runner's own verification per assembly | For that machine and run: same properties as the CI row | The receipt is not read by `validate_repository.py`; carrying it into a profile is a manual, self-declared step |
| Device profiles, capability plans, receipt template | `docs/device-lab/profiles/`, `test-plans/`, `device-receipt.template.json` | Device Lab author | `validate_device_lab_contract`, `validate_device_profile`, `validate_capability_test_plan`, `test_device_profiles_keep_claim_gates_and_no_capability_steps`, `test_not_tested_template_is_not_an_execution_receipt` | Schema-valid, ids equal filenames, claim gates fixed per profile, template stays `not_tested`, a `partial` or `verified` `evidence_status` needs a committed (passing) receipt | Hardware and input-route facts in a profile; plan step wording |
| Device execution receipts | `docs/device-lab/receipts/*.json` (none committed today); `--device-lab-receipt` | Tester | `validate_device_lab_execution_receipt`, `validate_device_runtime_binding`, and the `test_device_receipt_rejects_*` family | Full public-reachable SHA, artifact digest and manifest match, package tuple and lock graph equal the profile, build, runtime, device, and input tuples exact, required checks not `not_tested`, result derived from checks | Every observation, the tester identity, posture, and duration. All three profiles are `not_tested`: no device claim in the repository is machine-backed today |
| Editor evidence in a merge candidate | `check_unity_evidence` | Change author | `check_unity_evidence`, `test_receipt_binding_head_commit_satisfies_unity_evidence` | A `docs/validation/*.json` file in the change names a commit after which no package source changed | Reported as `info`, never blocking; a source change with no receipt still merges |
| Narrative validation reports | `docs/validation/*.md`, `docs/validation/experiments/*.md` | Author | None | Nothing | Every statement, including those `reference-catalog.json` lists as `evidence` (only the path's existence is checked) |
| Independent review receipts | `docs/validation/reviews/*.json` | Reviewer | `validate_task_hall_contract` calling `validate_independent_review_receipt`, `test_independent_review_receipt_requires_distinct_agents_and_closed_findings` | Schema-valid, `accepted`, executor and reviewer `assisted_by` sets are disjoint, `reviewed_commit` reachable from a public origin ref | That the review read anything, and its findings |

## Staging families

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Staging manifests | `staging/<family>/*/package.staging.json` | Family author | None: `discover_lingkyn_package_paths` reads only `package.json`, so staging is invisible to the catalog rules; `validate_foundry_contract` rejects any `.foundry-scaffold.json` under `packages/` | JSON parses; `scan_text_safety` finds no forbidden marker | Every manifest field |
| Staging status, test counts, and promotion steps | `staging/<family>/README.md` | Family author | None (`scripts/open_work.py` lists the README as a `staging_promotion` item; it is a board, not a check) | Nothing | "Authored, unexecuted" is itself a self-declaration; the counts of tests and the claim that they compile are unchecked |
| Staging code and tests | `staging/<family>/*/Runtime`, `Tests` | Family author | None (no `.meta`, namespace, or asmdef rule applies outside `packages/`) | Nothing | That the code compiles or the tests pass |
| Admission and blueprint drafts | `docs/standards/<family>/admission.draft.json`, `blueprint.draft.json` | Family author | None until copied into `docs/foundry/admissions/` or `blueprints/`, where the Foundry rules apply | Nothing | Every field |

## Foundry production line

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Production-line manifest, blueprint example, schemas | `docs/foundry/foundry-manifest.json`, `unity-package-blueprint.example.json`, `*.schema.json` | Maintainer | `validate_foundry_contract`, `test_foundry_v1_positive_contract_and_fast_structure_pass` | Schema-valid; the example stays `record_status: example`; the Foundry source manifest uses admitted official domains | Lifecycle and claim-ceiling text |
| System admissions | `docs/foundry/admissions/*.json` | Maintainer | `validate_foundry_contract` | Schema-valid, one record per family, the admitted family set equals the live system families exactly | The `checks` booleans, `evidence_basis` URLs, `decision`, and `rationale` are a recorded decision, not a derived fact |
| Admitted blueprints | `docs/foundry/blueprints/*.json` | Maintainer | `validate_foundry_contract`, `scripts/scaffold_unity_package.py::validate_blueprint` at scaffold time, `test_foundry_admitted_blueprint_requires_matching_system_admission` | Schema-valid, admission record exists and names the same family and `source_manifest` | `positive_sources` membership is checked only when a scaffold is generated; `intended_maturity` and `implementation_issue` |
| Batches and batch registry | `docs/foundry/batches/*.v1.json`, `batch-registry.v1.json` | Maintainer | `validate_foundry_contract`, `test_foundry_first_batch_rejects_membership_and_version_drift`, `test_foundry_first_batch_rejects_compatibility_profile_swap`, `test_foundry_batch_registry_rejects_cross_batch_package_duplication` | Unique ids, paths exist, state and `release_tag` agree between registry and file, each package's path, version, maturity, and `device_evidence` equal the catalog and `package.json`, its profile exists, is `verified`, and records that version, every catalogue package sits in exactly one batch | That the release tag exists in Git and points at the evidence commit (this clone carries no tags); `claim_boundary` text |
| Next-batch queue | `docs/foundry/queue/next-batch.json` | Maintainer | `validate_foundry_contract`, `test_foundry_next_batch_rejects_placeholder_actions` | Schema-valid, unique candidate ids, no `package_ids` reserved, no placeholder text in controlled fields | `state`, `objective`, and `source_requirements` |
| Release notes | `docs/releases/*.md` | Maintainer | None | Nothing | Pinned selectors, verified claims, and non-claims on the page (the batch file, not the page, is what the validator reads) |

## Governance (mandates, deliberations, RFCs)

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Operating mandates | `docs/governance/mandates/*.mandate.json` | Maintainer | `validate_operating_mandates`, `test_operating_mandates_validate_and_fail_closed`; `check_mandated_branch`, `test_mandated_branch_and_process_merge_eligibility` | Schema-valid, unique id, `issued_at <= not_before < expires_at`, a revoked mandate records `revoked_at`, `forbidden_actions` mention pull request, merge, and Unity Editor; a head branch matches an unexpired, unrevoked mandate's branch patterns | That the maintainer decision in `decision.basis` was made, that `trigger_ref` exists, and that the Agent stayed inside `allowed_actions` |
| Live deliberation records | `docs/governance/deliberations/*.json` | Deliberation author | `validate_live_deliberation_records` (registered after `validate_operating_mandates`; reuses `validate_governance_deliberation_metadata`), `tests/test_deliberation_records.py` (`test_live_records_pass`, `test_window_shorter_than_class_minimum`, `test_decided_before_window_close`, `test_process_decided_by_expired_mandate`, `test_process_decided_by_revoked_mandate`, `test_lazy_consensus_with_objection_delta`, `test_missing_referenced_path`, and siblings); `check_governance_boundary` still evaluates one record only when `--deliberation-record` is passed; `scripts/open_work.py` lists open records | Schema conformance (formerly self-declared) and window arithmetic (formerly self-declared): every live record matches `deliberation-record.schema.json`, its `id` equals the uppercased filename stem and is unique, it names a `decision_class` whose minimum window is derived from `governance-model.v1.json` (a record without a class is reported, never guessed), `review_opened_at`, `review_not_before`, and `decided_at` are RFC 3339 UTC and ordered with the window at least the class minimum; `status`, deltas, decision, and lazy-consensus resolution (formerly self-declared): a resolved record's `decided_at` is not before `review_not_before`, a `process:<mandate_id>` decision names a mandate file whose id matches and that was in force, unexpired, and unrevoked at `decided_at`, no `risk` or `counterexample` delta exists on a lazy-consensus record, a person-decided record names a `@github` identity per the schema, and every `blob/main/<path>` evidence link exists in the tree | That the decision text, rationale, and synthesis are true; that an evidence URL says what the record attributes to it (links are not fetched); that an open record past its window is acted on (the validator reports errors only, so a pending resolution is not surfaced); that the steward's lazy-consensus commit was made by the mandated Agent rather than by anyone editing the file |
| Governance model and Agent membership model | `docs/governance/governance-model.v1.json`, `agent-membership-model.v1.json` | Maintainer | `validate_governance_contract`, `validate_agent_membership_contract`, `test_governance_contract_rejects_window_and_stage_drift`, `test_agent_membership_rejects_early_a2_to_a4_activation` | Schema-valid and pinned to `proposed`, stage `G0`, cell `G0xA0`, the 0/7/14-day windows, token-neutral authority, disabled external effects | Nothing further about the files; whether people follow them |
| Governance source manifests | `docs/governance/source-manifest.json`, `agent-native-source-manifest.json`, `docs/contributing/agent-commons-source-manifest.json` | Maintainer | `validate_governance_source_manifest`, `validate_agent_native_source_manifest`, `validate_agent_commons_source_manifest` | Required source ids present, HTTPS, classification admitted, lessons, limits, and exclusions non-empty, the pinned community reference stays pinned and excluded as DAO precedent | What each source actually says |
| RFC status lines | `docs/rfcs/*.md` | RFC author | `validate_governance_contract` (RFC 0004) and `validate_agent_membership_contract` (RFC 0006) require `Status: **Proposed**`; `is_proposed_rfc` in `check_governance_boundary` treats a newly added Proposed RFC as a proposal and any other RFC edit as a governance change, `test_new_proposed_rfc_is_a_proposal_not_a_rule_change` | RFC 0004 and 0006 cannot be marked adopted by text edit; editing an existing RFC needs a deliberation record | Status lines of RFC 0001, 0002, 0003, 0005, and 0007, including "in effect at prototype stage" |
| Task Hall contracts, registry, checkpoints | `docs/contributing/task-hall.v1.json`, `docs/contributing/tasks/` | Maintainer, contributors | `validate_task_hall_contract`, `validate_task_contract`, `validate_task_registry`, `validate_checkpoint_routing`, `validate_checkpoint_allowed_paths`, and the `test_task_*` family | Schema, lifecycle, allowed-path ownership, review routing, device-evidence references, no self-authorisation | That claimed work happened or that a lease was honoured |
| Code ownership and GitHub-side settings | `.github/CODEOWNERS`; branch protection, required checks, and auto-merge described in `GOVERNANCE.md` and `docs/contributing/merge-readiness.md` | Repository owner | `REQUIRED_AGENT_COMMONS_FILES` requires the CODEOWNERS file to exist | Existence | Its content, and every statement about GitHub configuration: nothing in the repository can observe whether `merge-readiness` is a required check or auto-merge is enabled |
| Agent guide boundary | `AGENTS.md` | Maintainer | `validate_agent_guide_source_boundary`, `test_agent_guide_rejects_existing_project_raw_material` | The guide names no existing project as raw material and contains no mojibake | All operating instructions |

## Merge readiness and CI

| Claim | Where it lives | Declared by | Machine check (function/test/job) | What the check proves | Still self-declared |
| --- | --- | --- | --- | --- | --- |
| Merge-readiness verdict | `scripts/merge_readiness.py`; job `merge-readiness` in `validate.yml`; `tests/test_merge_readiness.py` | Process | `check_no_merge_conflict`, `check_repository_contract` (runs the canonical contract command on the merged tree in a detached worktree), `check_version_bump_evidence`, `check_changelogs`, `check_maturity_unchanged`, `check_unity_evidence`, `check_governance_boundary`, `check_not_draft`, `check_independent_review`, `check_mandated_branch`, then `evaluate` | `ready` means every blocking check passed and none was `unknown`; `process_merge_eligible` additionally needs a routine class and a live mandate; the CI step "Require process-merge eligibility on mandated branches" fails a mandated branch that is not eligible | The verdict grants nothing (`authority.grants_merge_permission` is false). In CI `--reviews-informational` turns a missing human review into `info`, so no review ever blocks there; whether GitHub enforces the job is the GitHub-side row above |
| Repository contract in CI | Job `python-contract-matrix` (Python 3.11, 3.12, 3.13) and aggregate `repository-contract` in `validate.yml` | Process | `validate_repository_automation_contract`, `test_repository_automation_contract_rejects_matrix_and_dependabot_drift` | The workflow runs `python scripts/validate_repository.py --json --run-contract-tests` with full history, pinned dependencies, the fixed matrix, and a single aggregate check; Dependabot covers actions and pip monthly | Nothing further |
| Contract test suite | `tests/test_*.py` | Process | `run_contract_test_gate`, `test_contract_test_gate_is_fail_fast_and_propagates_test_failure` | The suite runs only after validation passes and a failure is a repository error | Nothing further |
| Workflow security | `.github/workflows/*.yml` | Workflow author | `validate_workflow_security`, `test_workflow_security_rejects_comment_trigger_and_unpinned_action` | Only `pull_request`, `push`, and `workflow_dispatch` triggers; `permissions` explicit and `read`/`none`; every external action pinned to a non-zero full SHA; checkout sets `persist-credentials: false` | Secret handling inside job steps beyond these rules |
| Public text safety | Every tracked or unignored text file | Everyone | `scan_text_safety`, `validate_active_repository_path_references`, `test_public_leakage_scan_covers_all_decodable_text_and_skips_binary` | No forbidden non-public marker, machine-local Windows path, or credential-shaped string; no legacy root paths | Any leak the marker list does not name |
| Assisted-by attribution | Commit trailers, `CONTRIBUTORS.md` | Committer | None | Nothing | Which tool and model assisted, and whether an accountable human identity stands behind the commit |

## Known gaps

The most consequential self-declared claims a machine could check next, ordered by
risk to the process-decides principle:

1. **Live deliberation records are structure-checked, not process-bound.** `validate_live_deliberation_records` now re-derives every live record's schema, window, decision timing, mandate validity, and objection state, so a wrong window, an impossible `decided_at`, or a lazy-consensus resolution under a dead mandate or over an objection fails the repository contract. What remains: the CI merge-readiness job still passes no `--deliberation-record`, so `check_governance_boundary` never binds a governance change in a pull request to the record that authorises it; an open record whose window has closed is not surfaced because the validator has no warnings channel; nothing proves that a lazy-consensus commit was made by the mandated Agent rather than by anyone editing the file; and evidence URLs are checked for existence in the tree, never for content. Passing the record path from the changed files in CI, and binding a resolution commit to the mandate's branch patterns, would close the first and the third.
2. **Independent human review is informational in CI.** `--reviews-informational` means a routine change on a mandated branch reaches `process_merge_eligible` with zero reviews. If this is intended (lazy consensus for routine work), the register and `GOVERNANCE.md` should say so explicitly; otherwise drop the flag or gate it on decision class.
3. **GitHub-side enforcement is invisible.** Whether `merge-readiness` and `repository-contract` are required checks and whether auto-merge is enabled cannot be observed from the tree. A read-only step that fetches the branch protection or rulesets via the REST API and compares them with a committed expectation would turn this into a check.
4. **Coverage-map clause semantics and audit commit.** `validate_coverage_map_claims` now proves that every named test, assembly, and count exists and agrees, but a test that exists is not a test that exercises its clause, and `audited_at_commit` is never bound to a public-reachable SHA (the localization map records `staging`). Binding the commit, and requiring each `covered` clause to cite a contract phrase that appears in the test body, would close the remaining gap.
5. **Non-Inventory API surfaces are still unvalidated; some source manifests remain self-declared behind an allowlist.** `validate_family_source_manifests` now checks every other family's `source-manifest.json` for schema shape, required admission fields, non-public markers, contract pairing, and a not-fetched review note, but the three `api-surface.md` inventories are still never compared with `Runtime/*.cs`, and `FAMILY_SOURCE_MANIFEST_UNVERIFIED_CLAIMS` still allowlists Design Language's missing verification contract, Foundations' missing source manifest, and the missing review note in Interaction, Persistence, Settings, and Design Language. Generalising `validate_inventory_api_baseline` to every family, and fixing those manifests instead of allowlisting them, would close the rest.
6. **Lesson dispositions are trusted.** An `adopted` status is never compared with code or tests; a family can answer every lesson with a sentence. A disposition could be required to cite a test name or contract clause that the coverage-map rule then verifies.
7. **Release tags are not bound.** The batch registry's `release_tag` is checked against the batch file only; nothing proves the tag exists or points at the evidence commit. A rule can compare `git tag --points-at` against the profile's `commit_sha` when the tag is fetched.
8. **Catalog evidence flags are free text.** `independent_compile`, `git_url_install`, and `device_evidence` could be derived from verified profiles and device receipts instead of typed.
9. **Reference-catalog maturity outside Inventory** is not cross-checked against the package catalog; extending `validate_inventory_projection_coherence` to every package artifact removes the gap.
10. **Evidence provenance.** Every Editor and device receipt is structure- and hash-checked, never origin-checked. The unity-consumer-tests job could publish a signed attestation of its NUnit result that a profile then references, so at least CI-produced evidence carries a provenance the validator can verify.

## How to update this register

Any new validator rule, merge-readiness check, CI job, or new kind of claim-bearing
artifact adds or amends a row here in the same change, naming the function, test,
or job exactly as it appears in the code and stating what the check does not
prove. When a self-declared row becomes machine-checked, move its text from the
"Still self-declared" column to the "What the check proves" column rather than
deleting it, so the history of what was once trusted stays readable. This register
is documentation: nothing validates it, its row counts, or its function names, and
a reader should grep for any name here before relying on it.
