# SemanticRouting

Renderer- and device-neutral intents routed from ordered, typed source frames with explicit contexts, policies, diagnostics, cancellation, and outcomes.

Import the sample from the Package Manager and inspect `SemanticRoutingExample.Run()` for a minimal registry, frame, and routing pass.
