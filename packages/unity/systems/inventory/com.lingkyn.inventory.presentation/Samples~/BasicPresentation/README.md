# Basic Inventory Presentation

This sample creates a Core aggregate, binds the renderer-neutral
`InventoryPresenter` to a recording view, and inspects the resulting immutable
view model. It intentionally contains no UGUI, UI Toolkit, XR, scene, or device
dependency.

Use a renderer package when a visible interface is required.
