# Lingkyn Settings Unity

`com.lingkyn.settings.unity` converts ScriptableObject catalogs and profiles into immutable Core registries and coordinates explicit consumer-provided applicators.

Runtime player choices remain in Core snapshots and persistence providers, not in authored assets.
