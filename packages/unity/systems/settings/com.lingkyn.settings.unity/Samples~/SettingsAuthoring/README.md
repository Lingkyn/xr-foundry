# SettingsAuthoring

Demonstrates ScriptableObject definitions converted into an immutable Core registry with explicit applicator wiring.

Call `SettingsAuthoringExample.Run(catalog)` after creating or loading a `SettingsCatalogAsset`.
